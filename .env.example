# --- Required (app fails fast if missing) ---
S3_BUCKET=artsy-atelier
CLOUDFRONT_DISTRIBUTION_ID=E123EXAMPLE

# --- Optional (defaults shown) ---
S3_REGION=us-east-1
PUBLIC_DOMAIN=artsy.dev
MAX_UPLOAD_BYTES=52428800
PORT=8080
